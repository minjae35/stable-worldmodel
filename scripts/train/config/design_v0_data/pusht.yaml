# @package data

environments:
  PushT:
    name: design_v0/pusht_expert_train.h5
    split_manifest: design_v0/splits/pusht.json
    frameskip: 5
    keys_to_load:
      - pixels
      - action
    keys_to_cache:
      - action
